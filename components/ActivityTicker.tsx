import React, { useEffect, useState } from 'react';
import { generateMockActivity } from '../services/mockService';
import { ActivityFeedItem } from '../types';

export const ActivityTicker: React.FC = () => {
  // Disabled for production launch until enough real activity exists
  return null;
};